spring:
   datasource:
      username: bar_os
      password: bar_os
      url: jdbc:mysql://localhost:3306/bar_os
   jpa:
      hibernate:
         ddl-auto: none
      show-sql: true
   sql:
      init:
         mode: never
         
